from django.apps import AppConfig


class PayChanguConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.compliance.paychangu'
    label = 'paychangu'
    verbose_name = 'PayChangu Payments'